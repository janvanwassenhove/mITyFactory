# {{app_name}} Testing Requirements

## Purpose

This document defines the testing requirements for {{app_name}}. All contributors must follow these guidelines.

---

## Test Categories

### 1. Unit Tests

**Required for**: All service functions, utilities, and models.

**Coverage targets**: 80%+

**Location**: `tests/unit/`

```python
# Example
def test_calculate_total_with_discount():
    result = calculate_total(100, discount=0.1)
    assert result == 90.0
```

---

### 2. Integration Tests

**Required for**: API endpoints and database operations.

**Location**: `tests/integration/`

```python
# Example
def test_create_item_endpoint(client: TestClient):
    response = client.post("/api/v1/items", json={"name": "Test"})
    assert response.status_code == 201
```

---

### 3. API Contract Tests

**Required for**: All public API endpoints.

Verify request/response matches OpenAPI spec.

---

## Test Quality Standards

### ✅ DO

1. Test one thing per test
2. Use descriptive test names
3. Test edge cases and error conditions
4. Keep tests fast and isolated
5. Use fixtures for common setup

### ❌ DON'T

1. Test implementation details
2. Write tests that depend on order
3. Use real external services
4. Hardcode test data inline
5. Skip error path testing

---

## Running Tests

```bash
# All tests
pytest

# With coverage
pytest --cov=src --cov-report=html

# Unit tests only
pytest tests/unit/

# Integration tests only
pytest tests/integration/
```

---

## Pre-Commit Checklist

- [ ] All tests pass
- [ ] Coverage maintained or improved
- [ ] Ruff lint passes
- [ ] Type hints complete

---

*Generated by mITyFactory*
