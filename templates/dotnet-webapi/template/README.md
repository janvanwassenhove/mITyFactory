# {{project_name}}

An ASP.NET Core 8 Web API generated by mITyFactory.

## Prerequisites

- .NET 8 SDK or later

## Getting Started

### Restore Dependencies

```bash
dotnet restore
```

### Run the Application

```bash
dotnet run
```

The API will start at http://localhost:5000 (HTTP) and https://localhost:5001 (HTTPS).

### API Documentation

Once running, access Swagger UI at:
- http://localhost:5000/swagger

### Health Check

- http://localhost:5000/health

## Development

### Project Structure

```
├── Controllers/           # API controllers
├── Models/               # Data models
├── Services/             # Business logic services
├── Program.cs            # Application entry point
├── appsettings.json      # Configuration
├── {{project_name}}.csproj  # Project file
├── Dockerfile            # Container build
└── .devcontainer/        # Dev container config
```

### Running Tests

```bash
dotnet test
```

### Building for Production

```bash
dotnet publish -c Release -o ./publish
```

## Container Deployment

Build the Docker image:

```bash
docker build -t {{project_name}}:latest .
```

Run the container:

```bash
docker run -p 8080:8080 {{project_name}}:latest
```

## Configuration

Configuration is in `appsettings.json` and can be overridden with environment variables.

Key configuration:
- `ASPNETCORE_URLS`: URLs to listen on
- `ASPNETCORE_ENVIRONMENT`: Environment (Development, Production)

## Stack

- **ASP.NET Core 8** - High-performance web framework
- **Minimal APIs** - Lightweight API endpoints
- **Swagger/OpenAPI** - API documentation
- **C# 12** - Modern language features

## License

Generated by mITyFactory
